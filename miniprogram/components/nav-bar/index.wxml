<view class="lxy-nav-bar {{extClass}}" style="background: {{backgroundColorTop}};height: {{customHeight === -1 ? navBarHeight+ navBarExtendHeight : ( customHeight !==0 ? navBarHeight+ navBarExtendHeight+ 38 : 0) }}px;">
	<view class="lxy-nav-bar__placeholder {{ios ? 'ios' : 'android'}}" style="padding-top: {{navBarHeight+ navBarExtendHeight}}px;visibility: hidden;"></view>
	<view class="lxy-nav-bar__inner {{ios ? 'ios' : 'android'}}" style="{{navigationbarinnerStyle}}{{displayStyle}}">
		<view class="lxy-nav-bar__left {{ title ? 'is_title' : '' }}" style="{{navBarLeft}}">
			<block>
				<slot name="left"></slot>
			</block>
		</view>
		<view class='lxy-nav-bar__center'>
			<block wx:if="{{title}}">
        <view wx:if="{{!titLoading}}" class='lxy-nav-bar__center-title'>{{title}}</view>
        <view wx:else class="lxy-nav-bar__center-spinner">
          <view class="three-bounce">
            <view class="one"></view>
            <view class="two"></view>
            <view class="three"></view>
          </view>
        </view>
			</block>
			<block wx:else>
				<slot name="center"></slot>
			</block>
		</view>
	</view>
	<view wx:if="{{!hideTip}}" class="lxy-nav-bar__add" style="top: {{navBarHeight+ navBarExtendHeight}}px;">
		<text>添加到我的小程序，刷题、学习更方便</text>
		<van-icon name="cross" custom-style="padding: 10px;" bindtap="closeTip" />
		<text class="arrows" style="right: {{arrowsRight}}px;"></text>
	</view>
</view>
