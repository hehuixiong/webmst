<view class="topic-page">
  <view class="sort-container"></view>
  <view class="topic-container">
    <topic-list
      data="{{topicList}}"
      topicSum="{{topicSum}}"
      loading="{{loading}}"
      noMore="{{noMore}}"
      type="{{type}}"
    ></topic-list>
  </view>
</view>