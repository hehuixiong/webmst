<view class="skills-container">
  <view class="skills-list">
    <block wx:for="{{skillsList}}" wx:key="index">
      <navigator class="skills-item" url="/pages/skills-res/skills-res?title={{item.title}}&id={{item.id}}&index={{index}}">
        <view class="tag">第{{index + 1}}题</view>
        <view class="title">{{item.title}}</view>
      </navigator>
    </block>
  </view>
</view>